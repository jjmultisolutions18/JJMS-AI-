{
  "entities": {
    "User": {
      "title": "User",
      "description": "Platform user with role-based access",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "description": "User email" },
        "displayName": { "type": "string" },
        "role": {
          "type": "string",
          "enum": ["ADMIN", "LINE MANAGER", "PROGRAMME COORDINATOR", "MENTOR", "INNOVATOR"]
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "Innovation": {
      "title": "Innovation",
      "description": "An innovation project or idea",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "problem": { "type": "string" },
        "solution": { "type": "string" },
        "targetMarket": { "type": "string" },
        "stage": {
          "type": "string",
          "enum": [
            "STAGE 0: Pipeline & Awareness",
            "STAGE 1: Ideation",
            "STAGE 2: Technology Development",
            "STAGE 3: Commercialisation"
          ]
        },
        "innovatorId": { "type": "string", "description": "UID of the owner" },
        "programmeId": { "type": "string", "description": "Associated programme ID" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "innovatorId", "stage"]
    },
    "Programme": {
      "title": "Programme",
      "description": "An innovation programme",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "coordinatorId": { "type": "string" },
        "managerId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "coordinatorId"]
    },
    "Evaluation": {
      "title": "Evaluation",
      "description": "Scoring for an innovation",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "innovationId": { "type": "string" },
        "evaluatorId": { "type": "string" },
        "scores": {
          "type": "object",
          "properties": {
            "innovation": { "type": "number", "minimum": 0, "maximum": 30 },
            "feasibility": { "type": "number", "minimum": 0, "maximum": 25 },
            "impact": { "type": "number", "minimum": 0, "maximum": 25 },
            "marketPotential": { "type": "number", "minimum": 0, "maximum": 20 }
          }
        },
        "totalScore": { "type": "number" },
        "swot": {
          "type": "object",
          "properties": {
            "strengths": { "type": "array", "items": { "type": "string" } },
            "weaknesses": { "type": "array", "items": { "type": "string" } }
          }
        },
        "recommendation": { "type": "string", "enum": ["Proceed", "Improve", "Reject"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["innovationId", "evaluatorId", "scores"]
    },
    "Project": {
      "title": "Innovation Project",
      "description": "Multi-stage innovation artifact",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "currentStage": { "enum": ["STAGE_0", "STAGE_1", "STAGE_2", "STAGE_3"] },
        "status": { "enum": ["ACTIVE", "PENDING_APPROVAL", "COMPLETED", "ON_HOLD"] },
        "innovatorId": { "type": "string" },
        "programmeId": { "type": "string" }
      }
    },
    "Programme": {
      "title": "Innovation Programme",
      "description": "High-level department programme",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "budget": { "type": "object" },
        "managerId": { "type": "string" }
      }
    },
    "Approval": {
      "title": "Approval Request",
      "description": "Workflow stage-gate request",
      "type": "object",
      "properties": {
        "projectId": { "type": "string" },
        "status": { "enum": ["PENDING", "APPROVED", "REJECTED"] },
        "type": { "type": "string" }
      }
    },
    "System": {
      "title": "System",
      "description": "System metadata and health checks",
      "type": "object",
      "properties": {
        "status": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/programmes/{programmeId}": {
      "schema": "Programme",
      "description": "Institutional programmes"
    },
    "/programmes/{programmeId}/cohorts/{cohortId}": {
      "schema": "Cohort",
      "description": "Cohorts within a programme"
    },
    "/projects/{projectId}": {
      "schema": "Project",
      "description": "Innovation projects"
    },
    "/approvals/{approvalId}": {
      "schema": "Approval",
      "description": "Approval workflow queue"
    },
    "/test/connection": {
      "schema": "System",
      "description": "Public connectivity test document"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/innovations/{innovationId}": {
      "schema": "Innovation",
      "description": "Innovation projects"
    },
    "/programmes/{programmeId}": {
      "schema": "Programme",
      "description": "Innovation programmes"
    },
    "/evaluations/{evaluationId}": {
      "schema": "Evaluation",
      "description": "Innovation evaluations"
    }
  }
}
